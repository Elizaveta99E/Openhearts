version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: Openhearts
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 7281
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: development
      DB_HOST: db  # Используем имя сервиса вместо localhost
      DB_PORT: 5432
      DB_NAME: Openhearts
      DB_USER: postgres
      DB_PASSWORD: 7281
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-network
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules

  adminside:
    build:
      context: ./adminside
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      NODE_ENV: development  # Для разработки лучше development
      VITE_API_URL: http://backend:8080/api  # Используем имя сервиса
    depends_on:
      - backend
    networks:
      - app-network
    volumes:
      - ./adminside:/usr/src/app
      - /usr/src/app/node_modules

networks:
  app-network:
    driver: bridge

volumes:
  db-data: